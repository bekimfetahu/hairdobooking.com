services:
  hairdobooking_frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
      target: production
    ports:
      - "3002"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    restart: unless-stopped
