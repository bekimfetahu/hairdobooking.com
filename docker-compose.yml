services:
  hairdobooking_frontend:
    build:
      context: .
      target: production
    ports:
      - "${APP_PORT:-3800}:3000"
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
