services:
  hairdobooking_frontend:
    build:
      context: .
      target: production
    ports:
      - "3002"   # Plesk maps container port externally
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3002
    restart: unless-stopped
