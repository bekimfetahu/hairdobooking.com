services:
  hairdobooking_frontend:
    build:
      context: .
      dockerfile: Dockerfile.qa
      target: production
    ports:
      - "3002"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3002
    restart: unless-stopped
