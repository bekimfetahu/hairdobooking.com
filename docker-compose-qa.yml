services:
  hairdobooking_frontend:
    build:
      context: .
      dockerfile: Dockerfile.qa
      target: production
    ports:
      - "3001"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3001
    restart: unless-stopped
